# git commands

git init - creates a new git repository
git status - check the changes made to the files inside the repository
git add - adding files to the staging areas
git log - view the recent commits
